{
  "name": "@adhd-planner/models",
  "version": "1.0.0",
  "description": "Generated GraphQL types and data access helpers",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "test:unit": "jest --passWithNoTests",
    "lint": "eslint src --ext .ts,.tsx",
    "lint:fix": "eslint src --ext .ts,.tsx --fix",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo node_modules"
  },
  "dependencies": {
    "aws-amplify": "^5.3.8"
  },
  "devDependencies": {
    "@types/node": "^18.16.0",
    "typescript": "^5.1.6"
  },
  "peerDependencies": {
    "aws-amplify": "^5.3.8"
  }
}