{
  "name": "@adhd-planner/utils",
  "version": "1.0.0",
  "description": "Shared utilities for ADHD Family Planner",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "test:unit": "jest --passWithNoTests",
    "lint": "eslint src --ext .ts,.tsx",
    "lint:fix": "eslint src --ext .ts,.tsx --fix",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo node_modules"
  },
  "dependencies": {
    "date-fns": "^2.30.0"
  },
  "devDependencies": {
    "@types/node": "^18.16.0",
    "typescript": "^5.1.6"
  }
}